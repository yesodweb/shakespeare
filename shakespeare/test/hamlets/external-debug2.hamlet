#{var}
#{id var}
@{url}
@{Home}
@{Sub SubUrl}
@?{urlp}
^{template}
$if id true
    true
    #{id $ id $ id (id $ id extra)}
$if not true
$else
    not true
    #{id $ id $ id (id extra)}
$if not true
$elseif true
    elseif true
    #{id $ id(id extra)}
$maybe j <- just
    #{j}
    #{id j}
    #{id (id extra)}
$maybe _ <- nothing
$nothing
    nothing
    #{id extra}
$forall l <- list
    #{id $ id l}
    #{extra}
